spring:
  application:
    name: traffiq

  config:
    import: optional:file:.env[.properties]

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info