// prettier.config.ts

import type { Config } from "prettier";

const config: Config = {
    semi: true,
    singleQuote: false,
    trailingComma: "all",
    tabWidth: 4,
    printWidth: 80,
};

export default config;
