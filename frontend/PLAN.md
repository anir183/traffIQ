# TraffIQ Frontend — Phase 0 + 1 Execution Tracker (throwaway)

Working tracker for the current session. Authoritative spec: `docs/Implementation_Plan.md`.
Updated live as work lands. Toggle from mock → live is a single env var: `VITE_DATA_SOURCE`.

**Status legend:** `[x]` done · `[~]` in progress · `[ ]` pending

---

## Decisions locked (session)

- Minimal shell composition (`ProtectedLayout` wraps existing `header.tsx` + `body.tsx`; no file moves).
- Header search (1F) built now, mock-backed, grouped results.
- Profile (1H): identity from `useAuth()` only; dropdown deferred to Phase 2.
- Notification bell re-wired to active alerts (`useAlerts`), not a separate notifications source (Phase 2).
- Phase-2-only material deferred (users/logs/notifications/settings/blacklist handlers + hooks + RequireRole).
- `segments` + `densityForecast` shipped as mock endpoints so Overview charts render identically.
- Dev proxy `/api` → `http://localhost:8080` added (inert until backend ships).
- Env defaults: `VITE_DATA_SOURCE=mock`, `VITE_API_BASE_URL=/api`, `VITE_AUTH_ENABLED=false`.
- **TomTom-backed mock data (Phase 1.5):** in `mock` mode, incident listings (management list/map + event stream + bell) come from real TomTom IncidentDetails v5 over a fixed Kolkata bbox; congestion + avg-speed (segment cards + summary metrics) come from Traffic Flow `flowSegmentData` over a fixed ~24-road geometry catalog. Mapped in new `src/api/tomtom/`; module-cached 90 s; concurrency-5 + timeouts on flow; `VITE_TOMTOM_API_KEY` unset / timeout / API error → fall back to static seeds (`alerts.ts`/`segments.ts`/`traffic.ts`), which are retained (not deleted). Mapping: severity high/medium → triage (mgmt list+map), low → event-stream (queue+bell); after Phase 1.17 every live incident carries an `event_stream` so the event stream, mgmt list, and map all mirror the same live incidents (static seeds still split system events → stream / incidents → triage). Only `avg_speed_kmh`/`congestion_score` overridden in summary (volumes/breakdown/per-camera/density stay static — not TomTom-derivable).

---

## Phase 0 — Foundation

| #   | File(s)                                                                                                                                                | Status |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| 0A  | `src/types/contract/{apiEnvelope,errorCodes,pagination,anprEvent,vehicle,trajectory,trafficSummary,alert,camera,auth,user}.ts`                         | [x]    |
| 0B  | `src/api/env.ts`, `src/api/http.ts`, `src/api/sources.ts` (+ `services/storage.ts`)                                                                    | [x]    |
| 0B  | `src/api/endpoints/{anpr,vehicles,cameras,traffic,alerts,auth,search}.ts`                                                                              | [x]    |
| 0B  | `src/api/mock/data/{anprEvents,vehicles,cameras,traffic,segments,densityForecast,alerts,users}.ts`                                                     | [x]    |
| 0B  | `src/api/mock/{handlers,middleware}.ts`                                                                                                                | [x]    |
| 0C  | `src/types/ui/{incidentStatus,severityRamp,adapters}.ts`                                                                                               | [x]    |
| 0D  | `src/auth/{types,AuthProvider,useAuth,ProtectedRoute}.tsx/.ts`                                                                                         | [x]    |
| 0E  | `src/hooks/{useAnprEvents,useCameras,useVehicles,useTrajectory,useAlerts,useTrafficSummary,useTrafficSegments,useTrafficDensityForecast,useSearch}.ts` | [x]    |
| 0F  | `src/pages/auth/LoginPage.tsx`                                                                                                                         | [x]    |
| 0G  | `src/components/layout/ProtectedLayout.tsx`, `App.tsx` routes, `main.tsx` AuthProvider                                                                 | [x]    |
| 0H  | `src/pages/NotFound.tsx`                                                                                                                               | [x]    |
| env | `.env.example`, `.env`, `vite.config.ts` proxy                                                                                                         | [x]    |

**Phase 0 gate:** lint + build clean; pages render identically (nothing re-wired yet).

---

## Phase 1 — Pluggable read surfaces

| #   | Surface          | Files                                                                                                                          | Status |
| --- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------ | ------ |
| 1A  | Overview         | `analysis/detailF`, `analysis/stataf`, `analysis/densityforecast`, `analysis/mapp` (heatmap→alerts), `analysis/incident-queue` | [x]    |
| 1B  | Live Feed        | `page2/feed`, `page2/updates`, `page2/camera-view`, `body/navigation` (circuits→cameras)                                       | [x]    |
| 1C  | ANPR             | `anpr/search`, `anpr/details`, `anpr/map`                                                                                      | [x]    |
| 1D  | Incidents        | `incident/incidents`, `incident/map`                                                                                           | [x]    |
| 1E  | Analysis         | `stat.tsx` (4 cards + range), `stat/cameraviewcount`, `stat/volume-speed`, `stat/vehicletype`, `stat/insights`                 | [x]    |
| 1F  | Header search    | `header/search.tsx` (`useSearch` dropdown) + `header.tsx`                                                                      | [x]    |
| 1G  | Bell             | `header/NotificationBell.tsx` → `useAlerts` (active count + 6 newest)                                                          | [x]    |
| 1H  | Profile identity | `header/profile.tsx` → `useAuth().user`                                                                                        | [x]    |

**Deleted (Phase 0/1):** [`x`] `analysis/useTrafficData.ts`, [`x`] `incident/incidentData.ts`, [`x`] `stat/analysisData.ts`, [`x`] `page2/cameraData.ts`, [`x`] `navigation` `AREA_NODES`.

**Restored for the mock map viewer:** [`x`] `components/map/incidentsApi.ts`, [`x`] `components/map/useViewportIncidents.ts`, [`x`] `src/config.ts` (`VITE_TOMTOM_API_KEY`), [`x`] dependency `@tomtom-org/maps-sdk`.

**Phase 1 gate:** [`x`] PASSED — lint + build clean; demo renders identical; no page imports `api/mock/` internals.

**Map render modes:** [`x`] `mapRenderMode` in `api/sources.ts` = `isMock ? "tomtom" : "custom"`. `mock` → real TomTom SDK viewer (`TomTomMap` + `useViewportIncidents` incidents heatmap via `HEATMAP_TOMTOM` + severity markers w/ popups; needs `VITE_TOMTOM_API_KEY`); `backend` → custom maplibre renderer (OSM raster + `HEATMAP_CUSTOM` red heatmap from `useAlerts()`). Shared `components/map/heatmap.ts` gates all source/layer mutations behind the map `load` event.

**Map view modes:** [`x`] analysis map has a 3-mode switcher (Traffic / Incidents / Nodes) — the Avg Speed mode was removed in Phase 1.18 (redundant next to Traffic). Modes toggle layers: traffic → congestion heatmap + flow-roads lines (SDK-native TomTom flow when keyed), incidents → severity markers only (no heatmap), nodes → lucide-react camera-icon symbols (SVG→addImage; circle fallback) over road-sample catalog points. Heatmap uses `["heatmap-density"]` only; traffic ramp blue→red (`HEATMAP_TOMTOM`). Camera icon is registered once on the map `load` event via `registerCameraIcon(map)`. Interactive flow layers (details in `docs/Implementation_Plan.md` §Phase 1.7 + §Phase 1.8 + §Phase 1.10): `flow-roads` line layer (real segment geometry + live speed/congestion from `fetchRoadFlow`) shown in every non-incidents mode so hovering anywhere along a road shows its flow popup; `flow-sample-points` / `flow-node-icons` layers in Nodes mode.

**Map interactions (Phase 1.7):** [`x`] hover/click detail popups on all map elements — incident markers (severity-colored pulse markers w/ popup on both the analysis + incident-management maps, `bindMarkerDetails`), road-flow lines/points (speed/free-flow/congestion/confidence via `bindFeatureHoverPopup`), and the incidents heatmap via its markers. Loading states surfaced via a pulsing `StatusPill` ("Loading incidents…") so the initial fetch delay is visible.

**Map overlays (Phase 1.8):** [`x`] hover anywhere along a road (not just sample points) — `flow-roads`/`flow-roads-casing` line layers built from `flowSegmentData.coordinates`, colored by congestion (red/amber/green, gray when no geometry). Heatmap paint tightened (radius 4→16 px across zoom 0→20, intensity ≤2.2, opacity 0.7) so incident clusters stay legible at low zoom. Popups show "No live flow data for this road" when a road has no segment geometry (no more perpetual "pending").

**List reliability (Phase 1.9):** [`x`] `useAlerts` polls every 30 s by default so the notification bell, Recent Incidents, incident-management map, event stream, and overview counts stay fresh (the TomTom 90 s cache dedups concurrent polls). Alert IDs are deterministic (coordinate-based fallback, no array index) to stop reconciliation flicker. Mock seed timestamps (`detected_at`, event-stream times) are generated relative to now instead of hardcoded Sept 2026 dates, so fallback/backend demos never look stale. Shared `components/ui/pagination.tsx` replaces the 5 duplicated inline paginators with a windowed page selector (`‹ 1 … n … m ›`, max ~7 buttons + ellipsis) so large page counts can never overflow the container.

**Map mode rework (Phase 1.10):** [`x`] Raster tile overlays (`FLOW_LAYER_RELATIVE`/`FLOW_LAYER_ABSOLUTE`, `ensureTrafficTiles`) removed — traffic and avg-speed modes now use heatmap built from `flowSampleFeatures` (`components/map/overlays.ts`). Congestion heatmap uses the existing blue→red ramp (`HEATMAP_TOMTOM`); speed heatmap uses a new red→amber→green ramp (`HEATMAP_FLOW_SPEED`) where point weight encodes speed so higher speed maps to higher density → greener color (the `applyHeatmapStops` helper swaps `heatmap-color` stops dynamically). Incidents mode shows markers only — no heatmap — with beefier severity dots (16 px, 3 px white border, drop shadow; `MAX_SEVERITY_MARKERS` raised from 12 to 40). Nodes mode shows a lucide-react `Camera` icon (`components/map/overlays.ts:registerCameraIcon` — SVG data-URI → `Image` → `map.addImage`; circle fallback if registration fails) via a `flow-node-icons` symbol layer instead of raw circles; the custom backend map (`CustomTrafficView`) uses the same flow heatmap pattern (null samples → blank) and alert-pulse markers for incidents instead of heatmap density. All modes registered in `bindFeatureHoverPopup` hover list including `flow-node-icons`.

**Map fix + region-linked dashboard (Phase 1.11):** [`x`] Two heatmap bugs fixed — `HEATMAP_FLOW_SPEED` now starts transparent at density 0 (was opaque red → uniform red wash over the whole map) and heatmap radius/intensity/opacity are much stronger (radius 14→35 px across zoom 0→20, intensity 1.2→3.5, opacity 0.85) so the sparse 24 road points render visible blobs. `flowHeatmapFeatures` assigns a baseline weight (0.15) when a road has no sample instead of 0, so traffic mode always shows something even before `fetchRoadFlow` resolves or when no TomTom key is set. Incident markers on the analysis map now use `addPulseMarker` with a new `size` param (`"sm" | "md" | "lg"`; analysis map uses `"lg"` — 24 px wrapper / 22 px dot, animated ping ring) instead of a static 16 px dot, matching the incident-management map's visibility.

**Viewport-linked dashboard stats (Phase 1.11):** [`x]` The overview cards (congestion index, top congested segments, avg speed by segment, traffic density) now filter by the region currently visible in the map. New `contexts/mapViewport.ts` (+ `MapViewportProvider.tsx`) holds the viewport bbox; both map views broadcast `bboxFromMap(map)` on `moveend` (300 ms debounce) via `bindViewportBroadcast`. `useTrafficSummary`/`useTrafficSegments`/`useTrafficDensityForecast` accept an optional `bbox` and re-fetch when it changes (via `useAsyncResource` deps). Mock handlers filter the TomTom flow samples with `filterByBbox` (`flow.ts` `pointInBbox`) before computing `summarizeFlow`/`congestedSegments`/`speedSegments`; density forecast scales its 3-hour curve by `roadFractionInBbox`. Segment contracts now carry optional `lat`/`lon`. A "Map region" badge appears on each card while a filtered view is active. No bbox (no key / initial load) → globally scoped stats as before.

**Continuous road heatmaps (Phase 1.12):** [`x`] Traffic/speed heatmaps were rendering as sparse isolated blobs because `flowHeatmapFeatures` emitted one Point at each road's static centroid — 24 pins city-wide — and never used the fetched road polylines. Now every vertex of `sample.coordinates` (the same `[lng, lat]` geometry the road-line layer already draws) becomes a heatmap feature carrying that road's severity weight, so the heatmap continuously traces the road network; roads without geometry still fall back to their centroid with the baseline weight. Paint retuned for continuous coverage (intensity capped at 2.8 at z15 instead of 3.5, opacity 0.7 instead of 0.85) so overlapping vertices read as ribbons rather than a solid wash. Key-less/custom mode stays centroid-dots by nature (no geometry available) — documented.

**Always-visible roads + status popup (Phase 1.13):** [`x`] Fixed the "The layer 'flow-roads' does not exist in the map's style" crash: `bindFeatureHoverPopup` now filters hover layer ids to the ones currently in the style (and never throws) instead of querying blind, and `applyMode` is decoupled from `registerCameraIcon` so road layers are guaranteed to be added on map `load`. Roads now always render even when a TomTom fetch fails or no key is set — `roadFallbackGeometry` (`flow.ts`) builds a deterministic schematic polyline through each road's centroid used whenever `sample.coordinates` is absent, so the heatmap traces roads and lines are queryable in every mode. A new invisible `flow-roads-hover` layer (14 px, opacity 0) gives a generous hover hit-area under the heatmap. The road popup now leads with a traffic-status chip — Road closed / Congested / Heavy traffic / Free-flow / No live data — derived from `roadClosure` + congestion %, followed by avg speed, free-flow, congestion, and confidence.

**Traffic + speed modes remade (Phase 1.14):** [`x`] Traffic/speed modes fully rebuilt (incidents/nodes untouched). **Traffic** = native TomTom `TrafficFlowModule` segments (the familiar SDK live-flow look). **Speed** = a continuous network-wide speed heatmap harvested from the same flow tiles. `api/tomtom/viewportFlow.ts` (new) owns the demo side of the seam: `startFlowModule` attaches the module hidden (`visible:false`, road-category filter) purely as a vector-tile data source, and `harvestFlowSamples` reads the live viewport segments via `map.querySourceFeatures` (source-layer resolved from the style, raw snake_case tile props mapped to the neutral `FlowSample` contract — free-flow = absolute/relative, confidence = relative×100, never throws). `components/map/flowHeatmap.ts` (new) densifies each polyline to ~200 m spacing so the red→amber→green (`HEATMAP_FLOW_SPEED`) kernels fuse into ribbons, with popup props attached. `heatmap.ts` now exports `HEATMAP_SOURCE_ID/LAYER_ID/HOVER_LAYER`; `ensureHeatmapSource` rebuilds source+layers wholesale if any piece is missing (kills the "heatmap breaks when switching" silent-skip) and adds a transparent 9 px `traffic-heatmap-hover` circle hit-layer. `interaction.ts` gained optional `pickFallback` so hovering the native TomTom segments (which own no managed layer) opens the same status popup (raw segment props normalized). `TomTomTrafficView.applyMode` is now a deterministic visibility matrix — traffic: module visible; speed: module hidden + speed heatmap; nodes: road lines; incidents: markers — with a debounced `sourcedata`/`moveend` rebuild for speed mode. Backend route: a later swap replaces only `startFlowModule`/`harvestFlowSamples` with a fetch returning the same `FlowSample[]`; heatmap, popups, and mode switching are source-agnostic.

**Speed heatmap clamped + context-loss hardening (Phase 1.15):** [`x`] Speed mode was rendering every harvested viewport segment (thousands of ~200 m points, duplicated across overscaled tiles) with a huge kernel — a full-screen wash that could blow the WebGL context; after a context loss MapLibre re-applies only the SDK style (`setStyle {diff:false}`), then our effects slam `addLayer`/`setData` mid-rebuild ("Style is not done loading… Rebuilding the style from scratch", spurious `line-color Expected an odd number of arguments`) in a churn loop, and nothing re-ran `applyMode` to recover. Fixes: `speedHeatmapFeatures` caps at `MAX_HEATMAP_POINTS = 2500` via an even global stride and narrows the weight clamp to `[0.15, 0.95]`; heatmap kernel clamped to ribbons (radius 10→24 across zoom 0→20, intensity 1.0→1.6, opacity 0.8). All style mutations are crash-proof: `ensureHeatmapSource`/`updateHeatmapData`/`applyHeatmapStops` and `syncFlowSamples` are `isStyleLoaded()`-guarded and wrapped in try/catch, so nothing throws during a style rebuild. Speed rebuild debounce raised to 350 ms with an `isStyleLoaded` check, and a `webglcontextrestored` listener re-runs `applyMode` so the map self-heals after a context loss. Result: speed mode reads as thin readable ribbons (whole network, slow sections in red) and pan/zoom no longer crashes.

**Speed ribbons restored + ghost-popup/nodes cleanup (Phase 1.16):** [`x`] Fixes the three regressions that followed 1.15. (1) **Speed went blank** — two failure classes: `ensureHeatmapSource` called `map.removeSource` while its own layers still referenced the source, which maplibre's `style.removeSource` throws on (style rebuilds keep type-checking low-level), leaving a half-dead heatmap; and the harvest 350 ms rebuild depended on a cascade of soft conditions (`isStyleLoaded`, source-layer resolution, tile-load events) so any missed signal stayed empty forever. Now `ensureHeatmapSource` removes the heatmap + hover layers _before_ removing the source, so the recreate is deterministic in any partial state, and `resolveFlowSourceLayer` only caches a source-layer name when a flow layer actually matched (no stale un-cached "Flow" fallback ever poisoning the WeakMap). A speed-mode watchdog (2 s tick, refresh when ≥6 s stale) guarantees the heatmap self-heals even if every style/tile event was missed. (2) **Random road-info popups** — `flow-roads-hover`, the invisible 14 px hit layer, had default `visibility: visible` and sat in the hover list for all modes, so hovering empty space in traffic/speed over the 24 static roads popped their status popups (the roads themselves are hidden outside nodes mode). `setFlowSamplesVisibility` now toggles the hover layer together with the road lines (live only when roads are visible), and `interaction.ts` gained `closeHoverPopups(map)` (live-popup WeakMap) called inside every `applyMode`, so no popup lingers across a mode switch. (3) **Node-mode white flow lines** — the 7 px white `FLOW_ROADS_CASING_LAYER` under `flow-roads` (the rejected casing aesthetic) is deleted (constant, layer, visibility toggle); nodes keeps colored status roads + sample points + camera icons. Kernel retuned for clearly visible ribbons without the wash: radius 0→12/12→18/16→22/20→28, intensity 1.5→2.2, opacity 0.85, weight floor 0.2 with 0.65 end taper.

**Live incident lists + SDK-independent Avg Speed (Phase 1.17):** [`x`] Two live-data fixes. (1) **Event stream / incident list were empty while the map had incidents** — `buildAlert` (`api/tomtom/incidents.ts`) only attached `event_stream` to low-severity incidents, so with a key set the real (high/medium) incidents never reached the "Event Stream" panel (`incidentStreamAlerts`) and the partition emptied the other side too. The severity gate is removed: every live incident now carries an `event_stream` (warning when `magnitudeOfDelay ≥ 2`, else neutral). A new `listIncidentAlerts` adapter (`tt_`-prefixed live alerts + non-stream static) feeds Recent Incidents / logs so both live panels mirror the map; mock/static behavior is unchanged (stream = system events, list = triage incidents). (2) **Avg Speed tab showed nothing** — it was the only view on the TomTom map fed by `harvestFlowSamples` (`querySourceFeatures("vectorTilesFlow")`); with the flow module hidden / tiles not loaded for that path there were zero features and no fallback, so the heatmap never painted. It now paints `flowHeatmapFeatures(samplesRef.current, "speed")` — the same `fetchRoadFlow` 24-road catalog that powers the overview Average-Speed chart — so it always renders (live speeds, schematic fallback geometry) regardless of SDK tile state. Dead tile-harvest code removed (`resolveFlowSourceLayer`, `harvestFlowSamples` in `viewportFlow.ts`) and `components/map/flowHeatmap.ts` deleted.

**Avg Speed mode removed + mock-driven Incident Management (Phase 1.18):** [`x`] Product direction. (1) **Avg Speed map mode deleted** — `MapMode` is now Traffic/Incidents/Nodes; the speed heatmap ramp (`HEATMAP_FLOW_SPEED`), speed refresh/watchdog plumbing, and the `flowHeatmapFeatures` "speed" variant are gone. (2) **Incident Management detached from TomTom** — a new stored/seed path (`mock.getStoredAlerts` → `getStoredAlerts` endpoint → `useStoredAlerts` hook) serves the static `mock/data/alerts.ts` corpus (vehicle + camera processed detections) to the incident list and map regardless of `VITE_TOMTOM_API_KEY`; the analysis overview + event stream keep live TomTom. "Incident Management" no longer duplicates the analysis "Event Stream" panel. (3) Static `TRIAGE` seeds gained `latitude`/`longitude` so the Incident Management map plots ~6 active seeded points across Kolkata.

**Incident map polish + congestion sensitivity (Phase 1.19):** [`x`] Four fixes. (1) The Incident Management map now mirrors the full 15-row list — `mapIncidentPoints(alerts, includeInactive)` plots every seeded detection (active = pulsing severity dot, investigating = solid, resolved = gray); the analysis map keeps the active-only default. (2) Alert hover popups fit their box (root `w-56`, `break-words` everywhere, popup `maxWidth: "15rem"`). (3) Overview panel renamed "Incident & Event Stream" → **"Event Stream"**. (4) `summarizeFlow` congestion index is now `round(0.5·mean + 0.5·p90)` of per-road `(1−current/free)·100` so busy corridors stop reading ~33/"Low" (overview bands recalibrated to ≥55 High / ≥25 Moderate / else Low).

**Header bell → Incident Management corpus (Phase 1.20):** [`x`] The navbar bell now polls `useStoredAlerts` and feeds from `listIncidentAlerts`, so its badge + dropdown match the Incident Management tab's seeded detections (no TomTom); "Show all incidents" still routes to `/incident`. The analysis "Event Stream" panel and overview keep their own (TomTom/live) sources.

**Mock camera feeds (Phase 1.21):** [`x`] Live Feed now renders actual "feeds" under mock: a simulated corridor renderer (`liveFeedSim.ts` pure module; `LiveFeedCanvas` component) animates on the detail player and shows a static frame on grid tiles, with vehicle density tracking the nearest live `fetchRoadFlow` congestion (per-camera deterministic seed). Feed source is a pluggable per-camera registry (`api/mock/data/feedSources.ts`): `snapshot` (still/MJPEG URL → `<img>`, 12 s refresh, `onError` → simulated fallback) and `hls` (`.m3u8`, playback pending — needs hls.js) paste in with zero component changes. Offline cameras (no feed entry) render "No signal" instead of a phantom feed, header badge says Offline, tiles keep the `<Video>` icon. Camera count cut **40 → 11** (`CAM_001`…`CAM_011`, 8 live + 3 offline) so the grid + counts read sensibly ("8 live · 11 cameras"). Watch: feed URLs must be HTTPS (page is), images need no CORS, hls needs CORS + hls.js.

**Real HLS feeds (Phase 1.22):** [`x`] HLS playback is live: new `hls.js` dep (code-split into its own lazy chunk, loaded only when an HLS camera renders) + `components/camera/VideoFeed.tsx` — native HLS on Safari/iOS via `video.src`, otherwise `Hls.loadSource`/`attachMedia`, destroyed on unmount/URL change; fatal `Events.ERROR`, no URL, or unsupported → renders the simulated canvas fallback (a flaky feed never shows a black box). A dedicated external camera set (`LIVE_FEEDS` in `feedSources.ts`, ids `EXT_xxx`, circuit "External Feeds") is appended to the 11 local cameras and derived from entries that have a URL — adding a real stream is one data line, no code. Seeded: `EXT_001` "San Luis Reservoir" (Caltrans `wzmedia` `.m3u8`). Grid tiles auto-play a **muted live video thumbnail** + LIVE pill; the detail player adds controls. Note: `wzmedia.dot.ca.gov` is CORS-open (`ACAO: *`) but 403s datacenter IPs (bot-block) — playback depends on the viewer's network and failure falls back to the simulator.

**Real-feed-first visibility (Phase 1.23):** [`x`] When real feeds are configured **and** the frontend is online, only the external live cameras are shown — the simulated/offline local demo set (`SIMULATED_CAMERAS` 8+3) is hidden until either condition fails. `getVisibleCameras()` in `api/mock/data/cameras.ts` picks the set (`EXTERNAL_CAMERAS` vs full `CAMERAS`); the mock `getCameras`/`getCameraById` resolve against it (hidden simulated cams 404 / disappear from grid, circuits, overview counts); `useCameras` re-fetches on `window` `online`/`offline` so the grid toggles live when connectivity flips.

**Mock API simulation:** [`x`] `mockDelay()` in `api/mock/middleware.ts` is the single choke point all mock handlers funnel through. It applies random latency (`VITE_MOCK_LATENCY_MIN_MS`/`MAX_MS`, default 300–700; `MAX=0` disables) and a random failure rate (`VITE_MOCK_FAILURE_RATE`, default 5, percent; `0` disables). Auth handlers (`login`/`refresh`/`logout`/`getMe`) opt out of failure injection (`{ failure: false }`) so boot/login is deterministic; latency still applies. Endpoint files wrap mock branches with `abortable(mock…, signal)` so aborted in-flight calls settle immediately (parity with real `fetch`). `useAsyncResource` got a per-run "latest wins" id guard so stale resolutions (search-as-you-type, refetch, future polling) never overwrite newer data. Pages surface loading/error via `components/ui/fetch-status.tsx` (`InlineFetchStatus`) instead of misleading empty states.

**TomTom-backed mock subsets (Phase 1.5):** [`x`] `getAlerts`/`getSegments`/`getTrafficSummary` in `api/mock/handlers.ts` now call TomTom first (details in `docs/Implementation_Plan.md` §Phase 1.5): incidents via `fetchIncidents` (city bbox, 90 s cache, 10 s timeout), traffic/speed via `flowSegmentData` over a fixed ~24-road geometry catalog (concurrency 5, 8 s per-call timeout, per-road failure skipped, 90 s cache). No key / timeout / API error → static seed fallback (unchanged rendering). `src/api/tomtom/keys.ts` guards the key; `flow.ts` and `incidents.ts` own fetch + mapping. Static `mock/data/{alerts,segments,traffic}.ts` retained as fallbacks.

---

## Cross-cutting rules

- Verify `npm run lint && npm run build` after every slice.
- eslint react-hooks v7: no setState synchronously in effects; full deps.
- `verbatimModuleSyntax` → type-only imports use `import type`.
- Pure adapters in `types/ui/` (no React). Hooks own fetch + abort lifecycle.
- Map viewers: mock mode ships `@tomtom-org/maps-sdk` (real traffic viewer; `VITE_TOMTOM_API_KEY` in gitignored `.env`); `maplibre-gl` stays for the backend/custom renderer + incident/ANPR maps. No new dependencies beyond these.
